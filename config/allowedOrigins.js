// to request data from other domain (ie: google.com)
// create a list that is allowed to access the backend (REST API)
const allowedOrigins = [
    'http://localhost:5173' //React JS local url'
]

module.exports = allowedOrigins